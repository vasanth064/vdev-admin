import { createTheme } from '@mui/material/styles';

const vdevAdminTheme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#bd93f9',
    },
    secondary: {
      main: '#50fa7b',
    },
    background: {
      paper: '#282a36',
    },
  },
});

export default vdevAdminTheme;
